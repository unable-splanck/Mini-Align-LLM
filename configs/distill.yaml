seed: 42
teacher:
  model_path: checkpoints/ppo

student:
  name_or_path: distilgpt2

data:
  distill_file: data/processed/distill_train.jsonl
  max_length: 512

training:
  output_dir: checkpoints/student
  per_device_train_batch_size: 2
  learning_rate: 5.0e-5
  num_train_epochs: 1

